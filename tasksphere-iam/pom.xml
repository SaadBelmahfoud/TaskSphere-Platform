<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.tasksphere</groupId>
        <artifactId>tasksphere-platform</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>tasksphere-iam</artifactId>
    <name>TaskSphere IAM</name>

    <dependencies>
        <!-- Sécurité -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <!-- Web (Nécessaire juste pour le @RestController de AuthController) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <!-- Base de données -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- ===== Base de données : H2 (DEV uniquement) ===== -->
        <!-- Scope "test" : H2 n'est disponible QUE dans les tests.
             En production, on utilise PostgreSQL. Même si le module IAM n'a pas
             sa propre config, le driver PostgreSQL doit être présent car les
             entités IAM (UserEntity, RefreshTokenEntity) sont scannées par
             l'application Core au démarrage. -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ===== AJOUT SPRINT 5 : PostgreSQL JDBC Driver ===== -->
        <!-- PRINCIPE : Le driver JDBC permet à Java de communiquer avec PostgreSQL.
             Même si le module IAM n'a pas sa propre configuration YAML, les entités
             JPA (UserEntity, RefreshTokenEntity) sont scannées par le module Core
             au démarrage via @EntityScan. Hibernate a donc besoin du driver PostgreSQL
             pour valider les tables iam_users et refresh_tokens. -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- JWT (Utilisation de la variable définie dans le parent) -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- TESTS : JUnit 5, Mockito, AssertJ, Spring Test -->
        <!-- spring-boot-starter-parent gère la version automatiquement -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>