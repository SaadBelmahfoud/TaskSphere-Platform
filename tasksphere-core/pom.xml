<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.tasksphere</groupId>
        <artifactId>tasksphere-platform</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>tasksphere-core</artifactId>
    <name>TaskSphere Core</name>

    <dependencies>
        <dependency>
            <groupId>com.tasksphere</groupId>
            <artifactId>tasksphere-iam</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ===== Base de données : H2 (DEV uniquement) ===== -->
        <!-- Scope "test" : H2 n'est disponible QUE dans les tests.
             En production, on utilise PostgreSQL. Cela évite qu'H2
             soit accidentellement auto-configuré en production. -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ===== AJOUT SPRINT 5 : PostgreSQL JDBC Driver ===== -->
        <!-- PRINCIPE : Le driver JDBC permet à Java de communiquer avec PostgreSQL.
             Spring Boot auto-configure la connexion à partir des propriétés YAML.
             Le driver est en scope "runtime" car il n'est utilisé qu'à l'exécution,
             pas à la compilation (on utilise l'API JDBC standard). -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ===== AJOUT SPRINT 5 : Flyway — Migrations de base de données ===== -->
        <!-- PRINCIPE : Flyway exécute les scripts SQL (V1, V2, ...) dans l'ordre
             pour créer et versionner le schéma de la base de données.
             - flyway-core : moteur principal de migration
             - flyway-database-postgresql : support PostgreSQL (OBLIGATOIRE avec Spring Boot 3.x)
               Sans ce module, Flyway ne peut pas se connecter à PostgreSQL et échoue avec
               "Unsupported database: PostgreSQL" -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- ===== AJOUT SPRINT 5 : Spring Boot Actuator ===== -->
        <!-- PRINCIPE : Actuator expose des endpoints de monitoring et de santé.
             - /actuator/health : utilisé par Docker pour vérifier que l'app répond
             - /actuator/info : informations sur l'application
             - /actuator/metrics : métriques de performance
             Sans cette dépendance, les endpoints /actuator/* retournent 404. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
            <version>2.2.0</version>
        </dependency>
        <!-- ===== AJOUT : Swagger / OpenAPI ===== -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.6</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>